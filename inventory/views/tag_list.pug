extends layout 

block content 
  h1= title 

  ul 
    each tag in tag_list 
      li 
        a(href=tag.url) #{tag.name} 
    
    else 
      li There was an error retrieving the tag list from the database.